# Apologia

Project layout

/apologia
  /apps
    /web         (frontend)
    /api         (backend)
  /docs
  /samples       (sample pptx files)
  /scripts
  README.md

Notes
- Place frontend code in `apps/web` and backend code in `apps/api`.
- Put sample .pptx files in `samples`.
- Use `scripts` for utility scripts.
